#include <stdio.h>
int main()
{
    char variabileMe[35];
    printf("What's your name, bro?\n");
    scanf("%s",&variabileMe);
    printf("Welp, \"nice\" to meet ya. \"%s\"\n",variabileMe);
    return(0);
}